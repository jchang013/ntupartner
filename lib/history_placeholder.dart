import 'package:flutter/material.dart';

class PlaceholderHistory extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    final wordPair = 'No History Available';
    return Text(wordPair, textAlign: TextAlign.center,);
  }
}